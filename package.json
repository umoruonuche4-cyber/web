{
  "name": "secret-tree-hole-chat",
  "version": "1.0.0",
  "private": true,
  "description": "秘密的树洞：a password-protected remote chat website with photo and video sharing.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "pages:dev": "wrangler pages dev public",
    "pages:deploy": "wrangler pages deploy public --project-name web"
  },
  "engines": {
    "node": ">=18"
  }
}
